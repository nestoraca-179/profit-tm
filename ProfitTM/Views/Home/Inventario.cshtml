@{
    ViewData["Title"] = "Dashboard";
    ViewData["home"] = Session["home"];
}
<header class="container-fluid">
    <div class="row" style="height: 60px;">
        <div class="col-sm-2 header-left-side text-white">
            <i class="fas fa-bars icon-menu" id="icon-menu"></i>
            <a href="/Home/@ViewData["home"]">Profit Plus TM</a>
        </div>
        <div class="col-sm-10 header-right-side">
            <div class="user-active" role="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-user icon-user"></i>
                <p class="mb-0">@ViewBag.user</p>
            </div>
            <div style="width: 225px; text-align: center;" class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <a style="text-align: center; text-decoration: none;" href="/Account/Logout">Cerrar Sesión</a>
            </div>
        </div>
    </div>
</header>
<div class="container-fluid p-0">
    <div class="dash-container" id="dash-container">
        <div class="dash-left-side border-right p-0" id="menu-left">
            <ul class="list-menu dropright">
                @{
                    foreach (var item in ViewBag.options)
                    {
                        string disTable = item.UrlTable == "#" ? "disabled" : "";
                        string disProcess = item.UrlProcess == "#" ? "disabled" : "";
                        string disReport = item.UrlReport == "#" ? "disabled" : "";

                        <li class="menu-item" onclick="print(this)">
                            <i class="@item.Icon"></i>
                            <p class="mb-0 ml-3">@item.Name</p>
                        </li>
                        <div class="myDropdown">
                            <a class="dropdown-item @disTable" href="@item.UrlTable"><i class="fas fa-table"></i>  Tablas</a>
                            <a class="dropdown-item @disProcess" href="@item.UrlProcess"><i class="fas fa-sync-alt"></i>  Procesos</a>
                            <a class="dropdown-item @disReport" href="@item.UrlReport"><i class="fas fa-file"></i>  Reportes</a>
                        </div>
                    }
                }
                <li class="menu-item">
                    <i class="fas fa-cog fa-spin"></i>
                    <p class="mb-0 ml-3">Configuración</p>
                </li>
            </ul>
        </div>
        <div class="dash-right-side">
            <div class="container-fluid" style="padding: 5px 40px">
                <h2 class="mt-2 text-center m-4">TOP 10 Articulos mas vendidos</h2>
                <table class="table table-striped table-hover table-bordered">
                    <thead class="thead-dark">
                        <tr>
                            <th scope="col">Codigo</th>
                            <th scope="col">Nombre</th>
                            <th scope="col">Cantidad</th>
                            <th scope="col">Monto</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            foreach (var item in @ViewBag.mostSelledProds)
                            {
                                <tr>
                                    <td>@item.ID</td>
                                    <td name="name1">@item.Name</td>
                                    <td name="stock1">@item.Stock</td>
                                    <td>@item.Price</td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
                <div id="piechart"></div>
                <hr />
                <h2 class="mt-2 text-center m-4">TOP 10 Articulos mas comprados</h2>
                <table class="table table-striped table-hover table-bordered">
                    <thead class="thead-dark">
                        <tr>
                            <th scope="col">Codigo</th>
                            <th scope="col">Nombre</th>
                            <th scope="col">Cantidad</th>
                            <th scope="col">Monto</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            foreach (var item in @ViewBag.mostPurchasedProds)
                            {
                                <tr>
                                    <td>@item.ID</td>
                                    <td name="name2">@item.Name</td>
                                    <td name="stock2">@item.Stock</td>
                                    <td>@item.Price</td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
                <div id="donutchart"></div>
            </div>
        </div>
    </div>
</div>
@* Aqui empieza el script del PIECHART *@
<script type="text/javascript">
    google.charts.load('current', { 'packages': ['corechart'] });
    google.charts.setOnLoadCallback(drawChart);

    var names1 = document.getElementsByName("name1"), stocks1 = document.getElementsByName("stock1");
    var InfoChart1 = [["Producto", "Cantidad"]];

    for (let i = 0; i < 10; i++) {
        InfoChart1.push([names1[i].innerHTML, parseInt(stocks1[i].innerHTML)]);
    }

    function drawChart() {

        var data = google.visualization.arrayToDataTable(InfoChart1);

        var options = {
            is3D: true,
            height: 400,
            chartArea: {
                width: "100%",
                height: 500,
                bottom: 20
            },
            legend: {
                alignment: "center",
                textStyle: {
                    fontName: "Verdana"
                }
            },
        };

        var chart = new google.visualization.PieChart(document.getElementById("piechart"));

        chart.draw(data, options);
    }
</script>
@* Aqui termina el script del PIECHART *@
@* Aqui empieza el script del DONUTCHART *@
<script type="text/javascript">
    google.charts.load('current', { 'packages': ['bar'] });
    google.charts.setOnLoadCallback(drawDonut);

    var names2 = document.getElementsByName("name2"), stocks2 = document.getElementsByName("stock2");
    var InfoChart2 = [["Producto", "Cantidad"]];

    for (let i = 0; i < 10; i++) {
        InfoChart2.push([names2[i].innerHTML, parseInt(stocks2[i].innerHTML)]);
    }

    function drawDonut() {

        var data = google.visualization.arrayToDataTable(InfoChart1);

        var options = {
            height: 400,
            chartArea: {
                width: "100%",
                height: 500,
                bottom: 20
            },
            legend: {
                alignment: "center",
                textStyle: {
                    fontName: "Verdana"
                }
            },
            pieHole: 0.5
        };

        var chart = new google.visualization.PieChart(document.getElementById("donutchart"));
        chart.draw(data, options);
    }
</script>
@* Aqui termina el script del DONUTCHART *@

<script>
    var btn = document.getElementById("icon-menu");
    var menu = document.getElementById("menu-left");
    var node = null;

    btn.addEventListener("click", hide);

    function print(elem) {
        var elems = document.getElementsByClassName("myDropdown");

        for (var item of elems) {
            //item.style.display = "none";
            $(item).animate({ width: 'hide' });
        }

        if (node != null) {
            node.classList.remove("selected");
        }

        elem.classList.add("selected");

        if (node != elem) {

            $(elem).next().animate({ width: 'show' });
            $(elem).next().css("display", "flex");
            $(elem).next().css("flex-direction", "column");
            //elem.nextElementSibling.style.display = "flex";
            //elem.nextElementSibling.style.flexDirection = "column";
            menu.classList.add("active");
            node = elem;

        } else {

            node.classList.remove("selected");
            menu.classList.remove("active");
            node = null;

        }
    }

    function hide() {
        menu.classList.toggle("active");

        var elems = document.getElementsByClassName("myDropdown");

        for (var item of elems) {
            //item.style.display = "none";
            $(item).animate({ width: 'hide' });
        }
    }
</script>