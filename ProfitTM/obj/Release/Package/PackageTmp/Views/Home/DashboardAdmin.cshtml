@{
    ViewData["Title"] = "Dashboard";
    ViewData["home"] = Session["home"];
}
<style>
    .highcharts-figure, .highcharts-data-table table {
        min-width: 320px;
        max-width: 800px;
        margin: 1em auto;
    }

    .highcharts-data-table table {
        font-family: Verdana, sans-serif;
        border-collapse: collapse;
        border: 1px solid #EBEBEB;
        margin: 10px auto;
        text-align: center;
        width: 100%;
        max-width: 500px;
    }

    .highcharts-data-table caption {
        padding: 1em 0;
        font-size: 1.2em;
        color: #555;
    }

    .highcharts-data-table th {
        font-weight: 600;
        padding: 0.5em;
    }

    .highcharts-data-table td, .highcharts-data-table th, .highcharts-data-table caption {
        padding: 0.5em;
    }

    .highcharts-data-table thead tr, .highcharts-data-table tr:nth-child(even) {
        background: #f8f8f8;
    }

    .highcharts-data-table tr:hover {
        background: #f1f7ff;
    }

    .highcharts-credits {
        display: none;
    }

    .row.mt-3 {
        align-items: center;
    }
</style>
<header class="container-fluid">
    <div class="row">
        <div class="col-sm-2 header-left-side text-white">
            <i class="fas fa-bars icon-menu" id="icon-menu"></i>
            <a href="/Home/@ViewData["home"]">Profit Plus TM</a>
        </div>
        <div class="col-sm-10 header-right-side">
            <div class="user-active" role="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-user icon-user"></i>
                <p class="mb-0">@ViewBag.user.Descrip</p>
            </div>
            <div style="width: 225px; text-align: center;" class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <a style="text-align: center; text-decoration: none;" href="/Account/Logout">Cerrar Sesión</a>
            </div>
        </div>
    </div>
</header>
<div class="container-fluid p-0">
    <div class="dash-container" id="dash-container">
        <div class="dash-left-side border-right p-0" id="menu-left">
            <ul class="list-menu dropright">
                @{
                    foreach (var item in ViewBag.modules)
                    {
                        string disReport = item.ReportURL == "#" ? "disabled" : "";

                        <li class="menu-item" onclick="print(this)">
                            <i class="@item.Icon"></i>
                            <p class="mb-0 ml-3">@item.Name</p>
                            <i class="fas fa-caret-up"></i>
                        </li>
                        <div class="myDropdown">
                            @{
                                foreach (var elem in item.Options)
                                {
                                    string disabled = elem.Enabled ? "" : "disabled";

                                    <a class="dropdown-item @disabled" href="@elem.URL"><i class="@elem.Icon"></i> @elem.Name</a>
                                }
                            }
                            <a class="dropdown-item @disReport" href="@item.ReportURL"><i class="fas fa-file"></i>  Reportes</a>
                        </div>
                    }
                }
                <li class="menu-item" onclick="print(this)">
                    <i class="fas fa-cog fa-spin"></i>
                    <p class="mb-0 ml-3">Configuración</p>
                    <i class="fas fa-caret-up"></i>
                </li>
                <div class="myDropdown">
                    <a class="dropdown-item" href="/Usuarios/Index"><i class="fas fa-user"></i> Usuarios</a>
                </div>
            </ul>
        </div>
        <div class="dash-right-side">
            <div class="container-fluid" style="padding: 5px 40px">
                <h2 class="mt-2">Estadísticas</h2>
                <div class="row mt-3">
                    <div class="col-sm-3 direct">
                        <div class="col-md-12 shadow rounded">
                            <h4>Inventario</h4>
                            <i class="fas fa-box"></i>
                            <a href="Inventario">Ir a Inventario <i class="fas fa-arrow-circle-right"></i></a>
                        </div>
                    </div>
                    <div class="col-sm-3 direct">
                        <div class="col-md-12 shadow rounded">
                            <h4>Ventas</h4>
                            <i class="fas fa-dollar-sign"></i>
                            <a href="Ventas">Ir a Ventas <i class="fas fa-arrow-circle-right"></i></a>
                        </div>
                    </div>
                    <div class="col-sm-3 direct">
                        <div class="col-md-12 shadow rounded">
                            <h4>Compras</h4>
                            <i class="fas fa-tags"></i>
                            <a href="Compras">Ir a Compras <i class="fas fa-arrow-circle-right"></i></a>
                        </div>
                    </div>
                    <div class="col-sm-3 direct">
                        <div class="col-md-12 shadow rounded">
                            <h4>Configuración</h4>
                            <i class="fas fa-cog fa-spin"></i>
                            <a href="#">Ir a Configuración <i class="fas fa-arrow-circle-right"></i></a>
                        </div>
                    </div>
                </div>
                <hr />
                <div class="row mt-3">
                    <div class="col-sm-6">
                        <figure class="highcharts-figure">
                            <div id="container-msp"></div>
                        </figure>
                    </div>
                    <div class="col-sm-6">
                        <figure class="highcharts-figure">
                            <div id="container-mpp"></div>
                        </figure>
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-sm-6">
                        <figure class="highcharts-figure">
                            <div id="container-mac"></div>
                        </figure>
                    </div>
                    <div class="col-sm-6">
                        <figure class="highcharts-figure">
                            <div id="container-mmc"></div>
                        </figure>
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-sm-6">
                        <figure class="highcharts-figure">
                            <div id="container-mas"></div>
                        </figure>
                    </div>
                    <div class="col-sm-6">
                        <figure class="highcharts-figure">
                            <div id="container-mms"></div>
                        </figure>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="~/Scripts/bootstrap.min.js"></script>
<script>
    var session = @Html.Raw(Json.Encode(ViewBag.session));
    var capitalize = str => str.substring(0, 1).toLocaleUpperCase() + str.substring(1).toLocaleLowerCase();
    var btn = document.getElementById("icon-menu");
    var menu = document.getElementById("menu-left");
    var node = null;

    console.log(session);
    btn.addEventListener("click", hide);

    $(".dash-right-side").on("mousedown", function (event) {
        event.preventDefault();
    });

    $(".dash-right-side").on("contextmenu", function (event) {
        event.preventDefault();
    });

    function print(elem) {

        $(".myDropdown").slideUp();

        if (node != null) {
            $(node).removeClass("selected");
            $(node).children("i.fa-caret-up").hide();
        }

        $(elem).addClass("selected");

        if (node != elem) {

            $(elem).next().slideDown();
            $(elem).next().css("display", "flex");
            $(elem).next().css("flex-direction", "column");
            $(elem).children("i.fa-caret-up").show();
            node = elem;

        } else {
            $(elem).removeClass("selected");
            $(elem).children("i.fa-caret-up").hide();
            node = null;
        }
    }

    function hide() {

        if (node != null) {
            $(node).removeClass("selected");
            node = null;
        }

        menu.classList.toggle("active");

        $(".myDropdown").slideUp();
        $("i.fa-caret-up").hide();
    }

</script>

@* GRAFICA ARTICULOS VENDIDOS *@
<script>

    var productos = @Html.Raw(Json.Encode(@ViewBag.mostSelledProds));

    var objects = productos.map(function (x) {
        return {
            name: capitalize(x.Name),
            y: x.Stock
        };
    });
    objects.reverse();

    Highcharts.setOptions({
        lang: {
            decimalPoint: ",",
            thousandsSep: "."
        }
        /*colors: Highcharts.map(Highcharts.getOptions().colors, function (color) {
            return {
                radialGradient: {
                    cx: 0.5,
                    cy: 0.3,
                    r: 0.7
                },
                stops: [
                    [0, color],
                    [1, Highcharts.color(color).brighten(-0.3).get('rgb')] // darken
                ]
            };
        })*/
    });

    Highcharts.chart('container-msp', {
        chart: {
            type: 'column'
        },
        title: {
            text: 'Artículos más vendidos'
        },
        accessibility: {
            announceNewData: {
                enabled: true
            }
        },
        xAxis: {
            type: 'category',
            labels: {
                format: '<b>{value}</b>'
            }
        },
        yAxis: {
            title: {
                text: '<b>Cantidad</b>'
            },
        },
        legend: {
            enabled: false
        },
        plotOptions: {
            series: {
                borderWidth: 0,
                dataLabels: {
                    enabled: true,
                    format: '{point.y:,.2f} Unds'
                }
            }
        },
        tooltip: {
            pointFormat: '<span style="color:{point.color}">Cantidad</span>: <b>{point.y:,.2f}</b> Unds vendidas<br/>'
        },
        series: [{
            name: 'Cantidad',
            colorByPoint: true,
            data: objects
        }]
    });

</script>
@* GRAFICA ARTICULOS COMPRADOS *@
<script>

    var productos = @Html.Raw(Json.Encode(@ViewBag.mostPurchasedProds));

    var objects = productos.map(function (x) {
        return {
            name: capitalize(x.Name),
            y: x.Stock
        };
    });

    Highcharts.chart('container-mpp', {
        chart: {
            type: 'column'
        },
        title: {
            text: 'Artículos más comprados'
        },
        accessibility: {
            announceNewData: {
                enabled: true
            }
        },
        xAxis: {
            type: 'category',
            labels: {
                format: '<b>{value}</b>'
            }
        },
        yAxis: {
            title: {
                text: '<b>Cantidad</b>'
            }
        },
        legend: {
            enabled: false
        },
        plotOptions: {
            series: {
                borderWidth: 0,
                dataLabels: {
                    enabled: true,
                    format: '{point.y:,.2f} Unds'
                }
            }
        },
        tooltip: {
            pointFormat: '<span style="color:{point.color}">Cantidad</span>: <b>{point.y:,.2f}</b> Unds compradas<br/>'
        },
        series: [{
            name: 'Cantidad',
            colorByPoint: true,
            data: objects
        }]
    });

</script>
@* GRAFICA CLIENTES VENTAS *@
<script>

    var clientes = @Html.Raw(Json.Encode(@ViewBag.mostActiveClients));

    var objects = clientes.map(function (x) {
        return {
            name: capitalize(x.Name),
            y: x.Amount
        };
    });
    objects.reverse();

    Highcharts.chart('container-mac', {
        chart: {
            type: 'column'
        },
        title: {
            text: 'Clientes con más ventas'
        },
        accessibility: {
            announceNewData: {
                enabled: true
            }
        },
        xAxis: {
            type: 'category',
            labels: {
                format: '<b>{value}</b>'
            }
        },
        yAxis: {
            title: {
                text: '<b>Monto en ventas</b>'
            }
        },
        legend: {
            enabled: false
        },
        plotOptions: {
            series: {
                borderWidth: 0,
                dataLabels: {
                    enabled: true,
                    format: '{point.y:,.2f} Bs'
                }
            }
        },
        tooltip: {
            pointFormat: '<span style="color:{point.color}">Monto en ventas</span>: <b>{point.y:,.2f}</b><br/>'
        },
        series: [{
            name: 'Monto en ventas',
            colorByPoint: true,
            data: objects
        }]
    });

</script>
@* GRAFICA CLIENTES SALDO PENDIENTE *@
<script>

    var clientes = @Html.Raw(Json.Encode(@ViewBag.mostMorousClients));

    var objects = clientes.map(function (x) {
        return {
            name: capitalize(x.Name),
            y: x.Amount
        };
    });

    Highcharts.chart('container-mmc', {
        chart: {
            type: 'column'
        },
        title: {
            text: 'Clientes más saldo pendiente'
        },
        accessibility: {
            announceNewData: {
                enabled: true
            }
        },
        xAxis: {
            type: 'category',
            labels: {
                format: '<b>{value}</b>'
            }
        },
        yAxis: {
            title: {
                text: '<b>Saldo pendiente</b>'
            }
        },
        legend: {
            enabled: false
        },
        plotOptions: {
            series: {
                borderWidth: 0,
                dataLabels: {
                    enabled: true,
                    format: '{point.y:,.2f} Bs'
                }
            }
        },
        tooltip: {
            pointFormat: '<span style="color:{point.color}">Saldo pendiente</span>: <b>{point.y:,.2f}</b><br/>'
        },
        series: [{
            name: 'Saldo pendiente',
            colorByPoint: true,
            data: objects
        }]
    });

</script>
@* GRAFICA PROVEEDORES COMPRAS *@
<script>

    var proveedores = @Html.Raw(Json.Encode(@ViewBag.mostActiveSuppliers));

    var objects = proveedores.map(function (x) {
        return {
            name: capitalize(x.Name),
            y: x.Amount
        };
    });
    objects.reverse();

    Highcharts.chart('container-mas', {
        chart: {
            type: 'column'
        },
        title: {
            text: 'Proveedores con más compras'
        },
        accessibility: {
            announceNewData: {
                enabled: true
            }
        },
        xAxis: {
            type: 'category',
            labels: {
                format: '<b>{value}</b>'
            }
        },
        yAxis: {
            title: {
                text: '<b>Monto en compras</b>'
            }
        },
        legend: {
            enabled: false
        },
        plotOptions: {
            series: {
                borderWidth: 0,
                dataLabels: {
                    enabled: true,
                    format: '{point.y:,.2f} Bs'
                }
            }
        },
        tooltip: {
            pointFormat: '<span style="color:{point.color}">Monto en compras</span>: <b>{point.y:,.2f}</b><br/>'
        },
        series: [{
            name: 'Monto en compras',
            colorByPoint: true,
            data: objects
        }]
    });

</script>
@* GRAFICA PROVEEDORES SALDO PENDIENTE *@
<script>

    var proveedores = @Html.Raw(Json.Encode(@ViewBag.mostMorousSuppliers));

    var objects = proveedores.map(function (x) {
        return {
            name: capitalize(x.Name),
            y: x.Amount
        };
    });

    Highcharts.chart('container-mms', {
        chart: {
            type: 'column'
        },
        title: {
            text: 'Proveedores con más saldo pendiente'
        },
        accessibility: {
            announceNewData: {
                enabled: true
            }
        },
        xAxis: {
            type: 'category',
            labels: {
                format: '<b>{value}</b>'
            }
        },
        yAxis: {
            title: {
                text: '<b>Saldo pendiente</b>'
            }
        },
        legend: {
            enabled: false
        },
        plotOptions: {
            series: {
                borderWidth: 0,
                dataLabels: {
                    enabled: true,
                    format: '{point.y:,.2f} Bs'
                }
            }
        },
        tooltip: {
            pointFormat: '<span style="color:{point.color}">Saldo pendiente</span>: <b>{point.y:,.2f}</b><br/>'
        },
        series: [{
            name: 'Saldo pendiente',
            colorByPoint: true,
            data: objects
        }]
    });

</script>